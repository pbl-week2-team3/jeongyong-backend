const userTest = require('../routes/user');

const id = "menistream@gmail.com";
const nickname = "XPECTER";
const poassword = "1111";
const confirmPassword = "1111";
const profile_img_url = "asdf.png";

test('adds 1 + 2 to equal 3', () => {
    expect(sum(1, 2)).toBe(3);
});